%% =====================================================================
%% CRPTO -- INFORMS Journal on Data Science (IJDS) submission manuscript
%% =====================================================================
%% Double-anonymous official-template handoff synchronized with
%% paper/CRPTO_ijds.qmd. Preferred build:
%%
%%   latexmk -pdf -gg -interaction=nonstopmode CRPTO_ijds_submission.tex
%%
%% Robust Windows fallback:
%%
%%   pdflatex -interaction=nonstopmode -halt-on-error CRPTO_ijds_submission.tex
%%   bibtex CRPTO_ijds_submission
%%   pdflatex -interaction=nonstopmode -halt-on-error CRPTO_ijds_submission.tex
%%   pdflatex -interaction=nonstopmode -halt-on-error CRPTO_ijds_submission.tex
%%
%% Pass 1 writes .aux, BibTeX writes .bbl, pass 2 imports citations and
%% cross-references, and pass 3 stabilizes labels, floats, and pagination.
%% =====================================================================
\documentclass[ijds,dblanonrev]{informs4}

\usepackage{eqndefns-left}
\RequirePackage{tgtermes}
\RequirePackage{newtxtext}
\RequirePackage{newtxmath}
\RequirePackage{bm}
\RequirePackage{endnotes}
\OneAndAHalfSpacedXII
\TheoremsNumberedThrough
\EquationsNumberedThrough
\ECRepeatTheorems
\MANUSCRIPTNO{}

\usepackage{amsmath,amssymb,mathtools}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{placeins}
\usepackage{tabularx}
\graphicspath{{../../reports/crpto/figures/}}
\usepackage{natbib}
\bibpunct[, ]{(}{)}{,}{a}{}{,}
\def\bibfont{\small}
\def\bibsep{\smallskipamount}
\def\bibhang{24pt}
\def\newblock{\ }
\def\BIBand{and}

\begin{document}

\RUNAUTHOR{Anonymous}
\RUNTITLE{Marginal Conformal Coverage and Credit Portfolio Selection}

\TITLE{CRPTO: When Marginal Conformal Coverage Meets Maturity-Safe Credit
Portfolio Selection}

\ARTICLEAUTHORS{%
  \AUTHOR{}
  \AFF{}
}

\ABSTRACT{%
Marginal predictive coverage is often carried into optimization as if it
protected the selected decision. We test that implication in consumer credit,
where outcome maturity, temporal shift, and optimizer selection all intervene.
Conformal Robust Predict-Then-Optimize (CRPTO) combines a calibrated
probability of default (PD), an exact 90\% Mondrian split-conformal interval for
the binary repayment outcome, and a monthly loan-allocation linear program. We
construct a status-independent universe of 540{,}121 36-month Lending Club
loans, fit every statistical component by June 2012, select one guardrail on
mature July--December 2012 cohorts, and freeze it across 15 monthly decisions
from April 2016 through June 2017. Loans unresolved at the September 2020
snapshot remain in each menu and enter sharp binary bounds. All-candidate
coverage falls from 0.900448 at conformal fit to $[0.854923,0.879692]$ out of
time. Relative to a matched point-PD policy, the selected score
$q_i=0.75p_i+0.25u_i$ reduces the exposure-weighted default rate by
$[0.020093,0.046275]$, but lowers standardized realized payoff by
\$58{,}040--\$322{,}704 and increases funded-set miscoverage by
$[0.008822,0.029850]$. The default improvement comes from moving 61.13\% of
capital into the lowest conformal score stratum, not from improved selection
within strata. A transport identity shows that within-stratum optimizer
selection is instead the main source of the coverage failure. The evidence
supports a narrower conclusion than robust dominance: a conformal upper-score
constraint can act as a coarse portfolio-composition regularizer, while
marginal or Mondrian coverage does not control an adaptively selected funded
set under temporal shift.%
}

\KEYWORDS{conformal prediction; predict-then-optimize; credit risk; portfolio
selection; censoring; temporal validation; partial identification}

\maketitle

\section{Introduction}\label{sec:introduction}

Predictive models create value only through decisions, yet the property that
makes a predictor attractive need not survive the decision rule that consumes
it. A probability-of-default model can be well calibrated in a population
while an optimizer concentrates capital in a small, systematically different
subset. Likewise, a conformal interval can attain marginal or groupwise
coverage before selection while failing on the observations to which the
optimizer assigns positive weight. This distinction matters in credit because
the decision changes not just average risk but also interest-rate exposure,
portfolio composition, and which outcomes become economically consequential.

The usual retrospective workflow creates additional hazards. A loan that is
fully paid or charged off at the data snapshot has a label, whereas a current
loan may not. Filtering to labeled outcomes before constructing the candidate
menu uses future information that was unavailable at origination. Pooling
several years of originations into one allocation similarly lets a decision
made today choose from tomorrow's loans. Finally, optimizing one payoff and
evaluating another can make an apparent improvement an accounting artifact.
These are estimand defects rather than presentation details.

CRPTO studies the handoff from uncertainty quantification to a portfolio
decision after repairing those defects. A temporally trained CatBoost model and
Platt calibrator produce point PD $p_i$. An exact split-conformal recipe fitted
within five score strata produces a binary-outcome interval $[\ell_i,u_i]$.
The guardrail uses
\begin{equation}\label{eq:score}
q_i(\gamma)=(1-\gamma)p_i+\gamma u_i
\end{equation}
in the portfolio risk constraint, while both the guardrail and the point-PD
baseline maximize the same coherent expected payoff. The policy family is
intentionally small: $\tau\in\{0.15,0.17,0.19\}$ crossed with
$\gamma\in\{0.25,0.50,0.75\}$. One policy is selected on six mature 2012
development months and then applied, without refitting or reselection, to each
of 15 later monthly menus.

The empirical result is mixed and informative. The conformal guardrail buys a
sign-robust reduction in funded default, but it gives up payoff and does not
buy funded-set coverage. In fact, its selected-set miscoverage is worse than
the point-PD comparator. The mechanism is not a paradox. The upper-score
constraint shifts capital toward the lowest PD stratum, which improves default
through composition. Within each stratum, however, the optimizer still selects
loans by expected payoff, and that adaptive reweighting dominates the coverage
transport error.

The paper makes four contributions. First, it provides a maturity-safe,
monthly, temporally separated protocol for evaluating a
prediction-to-optimization policy. Candidate membership uses issue date and
contractual term, never final loan status, and no 2016--2017 outcome enters
model fitting or policy selection. Second, it aligns optimized and evaluated
quantities through the standardized binary payoff
$(1-Y_i)r_i-Y_i\lambda$ and retains unresolved outcomes with sharp aggregate
and pairwise bounds. Third, it introduces an exact transport decomposition that
separates population drift, exposure weighting, conformal-group composition,
and within-group optimizer selection for both default and miscoverage. Fourth,
it documents a practically relevant negative result: marginal Mondrian
coverage and a conservative conformal score can change the portfolio, but
neither implies selected-set validity or economic dominance.

This is a code-locked retrospective temporal audit, not a prospective trial or
a causal estimate. Its purpose is to identify what the conformal layer changes,
what it does not guarantee, and which evidence a decision-focused data-science
claim must expose.

\section{Related Work}\label{sec:related}

\subsection{From Predictive Quality to Decision Quality}

IJDS research repeatedly distinguishes estimation quality from the quality of
the action induced by an estimate. \citet{fernandezloria2022causaldecision}
show that a useful decision ranking and an accurate effect estimate are
different objects, and \citet{fernandezloria2025observational} make the
identifying assumptions behind observational decision rules explicit.
Cost-aware calibration similarly evaluates probability errors through their
downstream asymmetry rather than through calibration alone
\citep{yang2025costaware}. Credit graph models demonstrate the journal's
expectation that methodological choices, empirical design, and
reproducibility be linked \citep{das2023creditgraph}. More broadly, the AI--OR
interface is valuable when prediction, mathematical optimization, and
operational interpretation form one auditable chain \citep{wiberg2025ai_or}.

Decision-focused learning trains predictors against an optimization loss
\citep{donti2017,elmachtoub2022,mandi2024}. Contextual optimization and
predict-then-optimize methods instead preserve a modular predictor and expose
how forecast errors enter the decision
\citep{bertsimas2020prescriptive,sadana2025contextual}. CRPTO takes the modular
route because the research question concerns governance of a frozen score, not
a new credit-scoring leaderboard. The baseline and guardrail therefore share
the model, payoff, budget, concentration limits, solver, and monthly candidate
menus.

\subsection{What Conformal Coverage Does and Does Not Transport}

Split conformal prediction supplies finite-sample marginal coverage under
exchangeability \citep{vovk2005,angelopoulos2023}. Exact conditional coverage
is generally unattainable without strong restrictions \citep{barber2021limits},
and departures from exchangeability require an explicit discrepancy,
weighting, or adaptation mechanism
\citep{tibshirani2019covshift,gibbs2021aci,barber2023beyond,
farinhas2024nonexchangeable_crc}. Selecting among valid conformal objects can
itself invalidate them; recent work constructs controlled selection procedures
precisely because validity is not closed under arbitrary data-dependent choice
\citep{hegazy2025valid_selection_conformal_sets}.

Conformal uncertainty sets have also entered robust and contextual
optimization \citep{johnstone2021,patel2024}. The current frontier increasingly
calibrates decision loss, operational violations, or the
miscoverage--regret frontier rather than using predictive coverage as a proxy
\citep{yeh2025training,zhou2026creme,
stratigakos2026decision_calibrated_sets}. CRPTO does not claim a new
selected-set theorem. It asks a complementary empirical question: when a
simple conformal score is attached to a conventional credit LP, where does its
apparent risk effect come from, and where does coverage fail to follow?

\subsection{Credit Maturity and Economic Evaluation}

Credit scoring and profit scoring are related but distinct. High
discrimination does not determine which loan is profitable
\citep{lessmann2015}, and Lending Club studies show that interest rate,
default, recoveries, and portfolio constraints jointly shape investment
decisions \citep{serrano2016profitscoring,lyocsa2022profit}. Recent
uncertainty-aware profit models reinforce the need to evaluate the economic
target directly \citep{xu2024profit_risk_credit,xu2025profit_uncertainty_credit}.

Maturity is equally central. Ignoring random censoring can bias empirical risk
\citep{ausset2022censoring}. In online lending, default and prepayment are
competing events whose timing changes portfolio profitability
\citep{li2023online_loans}, and dynamic portfolio models track state
transitions and cash flows rather than a single binary reward
\citep{djeundje2025dynamic_loan_portfolio_profitability}. Our standardized
payoff is deliberately simpler. It isolates the decision effect of PD and
conformal uncertainty, but it is not an internal rate of return, discounted
cash-flow estimate, or substitute for survival analysis.

\section{Data and Locked Evaluation Design}\label{sec:data}

\subsection{Status-Independent Loan Universe}

The source is the Lending Club 2007--2020Q3 public research snapshot. The raw
file contains 2{,}925{,}493 rows; 2{,}060{,}077 have a 36-month contractual
term. We retain 540{,}121 rows in the declared temporal blocks after
deterministic schema, date, and feature checks. Candidate membership depends on
issue month, term, and fields observable at origination. It never depends on
whether the snapshot status later became resolved.

The binary target is \emph{snapshot default}. Status strings equal to
\texttt{Default} or containing \texttt{Charged Off} are positive;
\texttt{Fully Paid} is negative. Every other status is unresolved. This target
is a snapshot classification outcome, not a lifetime hazard or causal treatment
response. Unresolved loans remain candidates and are bounded after allocations
are frozen.

\begin{figure}[t]
\FIGURE
{\includegraphics[width=\textwidth]{crpto_ijds_ms_fig1_timeline.pdf}}
{Locked chronology. Statistical fitting and policy selection end in December
2012. Each 2016--2017 month is a separate decision with a fresh budget; no
future menu is pooled.\label{fig:timeline}}
{}
\end{figure}

The chronology in Figure~\ref{fig:timeline} separates six roles. The last
policy-selection month is December 2012 and the first primary evaluation month
is April 2016, a 40-month calendar separation. The primary window ends in June
2017, at least 39 months before the September 2020 snapshot for a 36-month
contract. Contract age nevertheless does not force administrative resolution,
which is why 11{,}386 primary candidates remain unresolved. July--September
2017 is retained as a more heavily censored extension rather than silently
discarded.

\begin{table}[t]
\TABLE
{Locked data blocks. Nondefaults complete the resolved counts.\label{tab:protocol}}
{\footnotesize
\begin{tabular}{lrrrrl}
\toprule
Block & Months & Rows & Defaults & Unresolved & Role \\
\midrule
PD development & 2007-06--2010-12 & 17{,}433 & 2{,}377 & 0 & fit/validate \\
Probability calibration & 2011-01--2011-12 & 14{,}101 & 1{,}499 & 0 & fit \\
Conformal fit & 2012-01--2012-06 & 14{,}967 & 2{,}063 & 0 & fit \\
Policy development & 2012-07--2012-12 & 28{,}503 & 3{,}840 & 0 & select once \\
Primary OOT & 2016-04--2017-06 & 376{,}890 & 57{,}662 & 11{,}386 & evaluate \\
Censored extension & 2017-07--2017-09 & 88{,}227 & 11{,}867 & 28{,}716 & stress \\
\bottomrule
\end{tabular}}{}
\end{table}

\subsection{Information Boundary}

The implementation materializes two ID-keyed panels. The decision panel
contains issue date, amount, purpose, contractual rate, point PD, conformal
endpoints, and frozen score stratum. It rejects outcome, realized-payoff,
miscoverage, or outcome-derived columns. The outcome panel contains snapshot
status and is joined only after the solver returns an allocation. IDs must
align one-to-one; partial joins fail. This physical separation does not create
a new statistical theorem, but it makes the timing claim testable in code.

The protocol and missing-outcome amendment were committed and tagged before
the successful primary result was written. A first tagged run stopped when its
predeclared guard found unresolved primary outcomes and persisted only a
protocol freeze. Version 2 changed only the handling of those missing outcomes
to sharp bounds; dates, model, policy grid, payoff, constraints, baselines, and
interpretation remained fixed.

\section{Method}\label{sec:method}

\subsection{Calibrated PD}

A CatBoost classifier uses 29 numeric and 9 categorical origination-time
features. Hyperparameters are fixed in the executable protocol: 500 trees,
depth 6, learning rate 0.04, class balancing, Bernoulli subsampling, and
time-aware ordering. The last 20\% of PD-development months form a temporal
validation tail. A logistic Platt map is fitted on 2011 raw margins, separate
from both model training and conformal fitting.

On 2011 calibration data, Platt scaling leaves AUC at 0.678214 while reducing
Brier score from 0.132582 to 0.091368 and ten-bin ECE from 0.181293 to
0.000711. On resolved primary OOT rows, AUC is 0.641688, Brier score 0.131126,
log loss 0.432369, and ECE 0.049691. These are diagnostics of substantial
temporal deterioration, not selection criteria.

\subsection{Exact Binary-Outcome Mondrian Intervals}

Let $Y_i\in\{0,1\}$ denote snapshot default and $p_i$ the calibrated score.
Five groups are defined by calibrated-PD quintiles on 2012H1. Within group $g$,
the conformity score is $s_i=|Y_i-p_i|$. For $n_g$ observations and target
$\alpha=0.10$, the frozen finite-sample rank is
\begin{equation}\label{eq:rank}
k_g=\left\lceil(n_g+1)(1-\alpha)\right\rceil,
\end{equation}
and $c_g$ is the $k_g$th ordered residual. A future score assigned to group
$g(i)$ receives
\begin{equation}\label{eq:interval}
[\ell_i,u_i]=
\left[\max\{0,p_i-c_{g(i)}\},\min\{1,p_i+c_{g(i)}\}\right].
\end{equation}
The interval predicts the observed binary outcome. It is not a confidence
interval for a latent individual PD. No holdout-learned widening, floor, or
post-2012 label enters the recipe.

For binary $Y$, miscoverage has the identity
\begin{equation}\label{eq:binary-miss}
m_i=\mathbf 1\{Y_i=0,\ell_i>0\}
 +\mathbf 1\{Y_i=1,u_i<1\}.
\end{equation}
Thus a default is counted as covered whenever $u_i=1$, even though such an
endpoint offers little discrimination to the optimizer. Conversely, a narrow
low-score interval with $u_i<1$ misses every realized default. This geometry is
central to interpreting funded-set coverage.

\subsection{Coherent Payoff and Monthly Allocation}

For contractual annual rate $r_i$ and fixed loss given default
$\lambda=0.45$, the standardized per-dollar realized payoff is
\begin{equation}\label{eq:realized-payoff}
\pi_i(Y_i)=(1-Y_i)r_i-Y_i\lambda,
\end{equation}
with conditional expectation
\begin{equation}\label{eq:expected-payoff}
\bar\pi_i=(1-p_i)r_i-p_i\lambda.
\end{equation}
The same expression is optimized and evaluated. It intentionally omits payment
timing, principal amortization, prepayment, fees, recoveries, and discounting.
We therefore call it standardized payoff, not cash-flow return or IRR.

In month $t$, let $a_{it}$ be dollar exposure to loan $i$, bounded by its listed
amount $A_i$. For a policy $(\tau,\gamma)$, the LP is
\begin{equation}\label{eq:lp}
\begin{aligned}
\max_{a_{it}}\quad & \sum_{i\in\mathcal I_t}a_{it}\bar\pi_i \\
\text{s.t.}\quad
& \sum_i a_{it}=B, \\
& \sum_i a_{it}q_i(\gamma)\le \tau B, \\
& \sum_{i:\,purpose_i=k}a_{it}\le 0.25B \quad\forall k,\\
& 0\le a_{it}\le A_i,
\end{aligned}
\end{equation}
where $B=\$1$ million in every month. The point-PD baseline sets $\gamma=0$.
The objective is identical across policies, so differences arise from the risk
score and loans made feasible by it.

\subsection{Development Selection and Locked Baselines}

Each of the nine guardrails is solved separately in every month from July
through December 2012. Among policies that invest the full budget in all six
months, the rule chooses the highest summed realized standardized payoff, then
expected payoff and candidate ID as deterministic tie-breakers. It selects
$\tau=0.17$ and $\gamma=0.25$, or $q_i=0.75p_i+0.25u_i$. A point-PD policy is
independently selected from $\tau\in\{0.15,0.17,0.19\}$ on the same development
months. All three point caps yield the same allocation because the point-risk
constraint is nonbinding; the deterministic tie-break selects $\tau=0.15$.
We additionally report a matched point policy at $\tau=0.17$. The two point
allocations remain identical in primary OOT.

The model, Platt map, conformal recipe, selected guardrail, and point baselines
are frozen. Every primary and extension month receives its own candidate menu
and fresh budget. No endpoint cap, future-menu pooling, or evaluation-period
reselection is used.

\subsection{Sharp Bounds for Unresolved Outcomes}

For an unresolved loan, $Y_i$ may be either zero or one. Default therefore lies
in $[0,1]$, payoff in $[-\lambda,r_i]$, and miscoverage in the two attainable
values implied by Equation~\eqref{eq:binary-miss}. Because each aggregate is
additive after an allocation is fixed, choosing the lower or upper attainable
contribution for each unresolved loan gives a sharp bound.

Policy contrasts require more care. CRPTO and point PD often fund different
loans, so subtracting two marginal intervals is generally not sharp. We form
the union of funded IDs, retain each loan's signed difference in exposure, and
optimize its common unresolved $Y_i$ once. The resulting contrast interval
respects shared outcomes and is sharp for the observed menus. A directional
claim is made only when the entire primary contrast interval has one sign.

\subsection{Selection-Transport Identity}

Let $M_{row}$ be the unweighted candidate metric, $M_{exp}$ its loan-amount
weighted value, $M_{mix}$ the value obtained by combining candidate
within-group rates with the funded portfolio's conformal-group shares, and
$M_{fund}$ the actual funded exposure-weighted value. For reference $a$,
\begin{equation}\label{eq:transport}
\begin{aligned}
M_{fund}-a={}&(M_{row}-a)+(M_{exp}-M_{row})\\
&+(M_{mix}-M_{exp})+(M_{fund}-M_{mix}).
\end{aligned}
\end{equation}
The terms are population departure from the reference, exposure weighting,
group-composition shift, and within-group optimizer selection. The identity is
exact by telescoping. With unresolved outcomes we evaluate it under lower and
upper extremal completions. The funded endpoints are sharp bounds; intermediate
terms describe each completion and are not component-wise confidence intervals.

\section{Results}\label{sec:results}

\subsection{Coverage Deteriorates Before Portfolio Selection}

The conformal fit block attains coverage 0.900448, as intended by the exact
finite-sample construction. In primary OOT, resolved-row coverage is 0.876313
and all-candidate coverage is bounded by $[0.854923,0.879692]$. Mean interval
width is 0.736564; 98.85\% of lower endpoints equal zero and 18.08\% of upper
endpoints equal one. The decline precedes portfolio optimization and already
rules out a 90\% OOT coverage claim.

\begin{table}[t]
\TABLE
{Binary-outcome conformal diagnostics. Bounds include every unresolved
candidate.\label{tab:coverage}}
{\footnotesize
\begin{tabular}{lrrrrrr}
\toprule
Block & Rows & Unresolved & Coverage & Width & $\ell=0$ & $u=1$ \\
\midrule
Conformal fit, 2012H1 & 14{,}967 & 0 & 0.900448 & 0.813817 & 0.991114 & 0.222757 \\
Primary OOT & 376{,}890 & 11{,}386 & [0.854923, 0.879692] & 0.736564 & 0.988458 & 0.180766 \\
Extension & 88{,}227 & 28{,}716 & [0.626804, 0.892539] & 0.729054 & 0.987986 & 0.179537 \\
\bottomrule
\end{tabular}}{}
\end{table}

The shift is also visible in the PD model. Primary default prevalence among
resolved candidates is 0.157760, materially above earlier blocks, while AUC and
calibration deteriorate. The conformal result should therefore be read as
empirical temporal evidence, not as a failed theorem under maintained
exchangeability.

\subsection{The Guardrail Lowers Default but Also Lowers Payoff}

On 2012H2 development data, the selected guardrail realizes \$581{,}408.64 on
\$6 million of monthly capital, versus \$531{,}148.54 for point PD. That
advantage does not transport. Across the 15 locked primary months, each policy
allocates \$15 million. The guardrail's expected standardized payoff is
\$2{,}383{,}112, compared with \$2{,}624{,}090 for point PD. Its realized
payoff is bounded by $[-\$2{,}389.90,\ \$168{,}250.04]$, whereas point PD is
bounded by $[\$177{,}108.53,\ \$369{,}495.74]$. Marginal intervals overlap,
but the paired sharp contrast does not.

\begin{table}[t]
\TABLE
{Primary monthly-policy aggregates. Payoff is the standardized quantity in
Equation~\eqref{eq:realized-payoff}.\label{tab:primary}}
{\footnotesize
\begin{tabular}{lrrrrr}
\toprule
Policy & Expected & Realized payoff & Default & Miscoverage & Unresolved exp. \\
\midrule
Conformal guardrail & \$2.383M & [-\$2{,}390, \$168{,}250] & [0.2928, 0.3103] & [0.2947, 0.3096] & 0.01755 \\
Point PD, matched $\tau$ & \$2.624M & [\$177{,}109, \$369{,}496] & [0.3260, 0.3434] & [0.2754, 0.2903] & 0.01739 \\
Point PD, selected $\tau$ & \$2.624M & [\$177{,}109, \$369{,}496] & [0.3260, 0.3434] & [0.2754, 0.2903] & 0.01739 \\
\bottomrule
\end{tabular}}{}
\end{table}

Relative to either point comparator, the guardrail's paired realized-payoff
difference is $[-\$322{,}703.79,-\$58{,}040.34]$, or
$[-0.021514,-0.003869]$ per dollar of capital. Its default-rate difference is
$[-0.046275,-0.020093]$, but its miscoverage difference is
$[0.008822,0.029850]$. All three signs are robust to unresolved outcomes. Thus
the guardrail produces a real default--payoff trade-off and a coverage loss,
not a free robustness gain.

\begin{table}[t]
\TABLE
{Sharp paired primary contrasts over 2{,}246 union-funded loans, 52 with
unresolved outcomes. These are retrospective, not causal, contrasts.
\label{tab:contrast}}
{\small
\begin{tabular}{lrrl}
\toprule
Guardrail minus point PD & Lower & Upper & Direction \\
\midrule
Realized standardized payoff & -\$322{,}703.79 & -\$58{,}040.34 & guardrail worse \\
Payoff rate & -0.021514 & -0.003869 & guardrail worse \\
Weighted default & -0.046275 & -0.020093 & guardrail better \\
Weighted miscoverage & 0.008822 & 0.029850 & guardrail worse \\
\bottomrule
\end{tabular}}{}
\end{table}

\subsection{The Result Is Heterogeneous Month by Month}

Figure~\ref{fig:monthly} reports every primary month. The guardrail has a
sign-robust payoff advantage in 5 months, a disadvantage in 8, and an ambiguous
contrast in 2. It has lower default in 12 months, higher default in 2, and an
ambiguous contrast in 1. For miscoverage, it is better in only 3 months, worse
in 10, and ambiguous in 2. Aggregation therefore does not hide a universal
monthly winner.

\begin{figure}[t]
\FIGURE
{\includegraphics[width=\textwidth]{crpto_ijds_ms_fig2_monthly.pdf}}
{Monthly policy outcomes in the locked primary window. Blue denotes the
conformal guardrail and orange point PD; shaded regions show sharp
unresolved-outcome bounds where the endpoints differ.
\label{fig:monthly}}
{}
\end{figure}

The final primary month has the largest unresolved share and correspondingly
wider bounds, but the aggregate payoff, default, and miscoverage signs remain
identified. The July--September 2017 extension is different: roughly 23.5\%
of funded exposure is unresolved for both policies. Its payoff and coverage
bounds are too wide for a directional promotion claim, so it is reported only
as a censoring stress test.

\subsection{Composition Explains the Default Improvement}

The guardrail and point policy use the same expected-payoff objective but fund
substantially different sets. The guardrail funds 1{,}561 unique loans, point
PD funds 1{,}421, and only 736 are shared. Its exposure-weighted point PD is
0.074979 versus 0.115758 for point PD. This safety shift comes with a lower
exposure-weighted contractual rate, 0.210114 versus 0.257957.

The most visible change is conformal-score composition. The guardrail places
61.13\% of exposure in the lowest score stratum and only 0.97\% in the highest;
point PD places 10.16\% and 10.43\%, respectively. This is the sense in which
the upper-score constraint acts as a coarse composition regularizer.

\begin{table}[t]
\TABLE
{Primary exposure shares by the five conformal score strata.
\label{tab:groups}}
{\small
\begin{tabular}{lrr}
\toprule
Score stratum & Guardrail & Point PD \\
\midrule
0, lowest calibrated PD & 0.611338 & 0.101627 \\
1 & 0.130426 & 0.232390 \\
2 & 0.159965 & 0.320223 \\
3 & 0.088615 & 0.241492 \\
4, highest calibrated PD & 0.009657 & 0.104268 \\
\bottomrule
\end{tabular}}{}
\end{table}

\subsection{Within-Group Selection Explains the Coverage Failure}

Figure~\ref{fig:transport} applies Equation~\eqref{eq:transport} under the lower
outcome completion. For default, the guardrail's composition term is -0.041210,
while point PD's is +0.019627. That difference accounts for the guardrail's
aggregate default advantage. Yet the within-group selection term is +0.175652
for the guardrail and +0.148077 for point PD. Both policies select loans with
much higher default than their stratum-level candidate counterfactual; the
guardrail does not repair that behavior.

\begin{figure}[t]
\FIGURE
{\includegraphics[width=0.93\textwidth]{crpto_ijds_ms_fig3_transport.pdf}}
{Transport from candidate rows to funded exposure under the lower completion.
The group-mix step explains the guardrail's lower default; the final
within-group step explains most funded-set miscoverage.
\label{fig:transport}}
{}
\end{figure}

For miscoverage, the candidate-row rate is 0.120308 and the loan-amount-weighted
rate is 0.126272. The guardrail's group mix would slightly improve it to
0.123598, but within-group selection adds 0.171111, yielding funded
miscoverage 0.294708. Point PD's group mix raises the rate to 0.131043 and its
within-group term adds 0.144317, yielding 0.275360. Under the upper completion,
the same qualitative conclusion holds. The coverage failure is therefore not
caused primarily by the guardrail's move toward low-score groups. It is caused
by adaptive selection within those groups, combined with binary interval
geometry and temporal drift.

\section{Discussion and Implications}\label{sec:discussion}

\subsection{What the Conformal Guardrail Actually Does}

The evidence supports a precise operational interpretation. Blending the
conformal upper endpoint into the risk constraint changes which score strata
receive capital. Because higher $u_i$ values are costly under the cap, the LP
shifts toward the lowest calibrated-PD stratum. That shift lowers realized
default, but it also removes high-rate loans and sacrifices standardized
payoff. Calling this behavior a composition regularizer is more accurate than
claiming a selected-set coverage guarantee.

The result also separates two meanings of conservatism. The guardrail is
conservative with respect to the score used in feasibility: $q_i\ge p_i$. It
is not statistically conservative on the funded set, where miscoverage is
higher than under point PD. Nor is it economically robust in a dominance sense,
because the entire paired payoff interval favors the baseline. A strong word
such as \emph{robust} must name the quantity and perturbation it protects
\citep{morucci2022robust_matching_uncertainty,falconer2026replication}. Here it
describes a transparent uncertainty-sensitive constraint, not distributional
robustness or a theorem under adaptive selection.

\subsection{Lessons for Decision-Focused Data Science}

Three design lessons generalize beyond credit. First, candidate construction
is part of the decision rule. Excluding rows because their outcomes are
unavailable creates a menu that could not have existed at decision time.
Keeping the rows and bounding their outcomes is often more honest than
obtaining a sharper answer from a selected sample.

Second, predictive validity and decision validity require different evidence.
Population coverage, group coverage, and selected-set coverage are distinct.
The transport identity makes that distinction measurable by showing whether a
failure arises from temporal drift, unit-size weights, group composition, or
within-group selection. If selected-set reliability is the desired guarantee,
one should calibrate an operational loss or use a selection-valid construction,
not infer it from marginal coverage.

Third, the optimized payoff must be the payoff later reported. A minor-looking
algebraic mismatch can reorder the portfolio. We use
$(1-p)r-p\lambda$ because it is exactly the expectation of
$(1-Y)r-Y\lambda$. A richer study should replace both sides together with
cash flows, prepayment, recovery, and discounting rather than relabel this
standardized outcome as investor return.

For a credit committee, the practical output is not a single headline number.
It is a trade-off table: expected and realized payoff bounds, default bounds,
funded-set miscoverage, unresolved exposure, temporal reversals, and portfolio
composition. The upper-score guardrail may be useful when reducing default is
worth a transparent economic cost. The current experiment does not identify
that preference or establish that the trade is optimal.

\section{Limitations and Reproducibility}\label{sec:limitations}

The study has seven main limitations. First, Lending Club is one historical
platform whose retail originations ended in 2020; the design cannot establish
live performance. Second, snapshot default collapses payment paths into a
binary endpoint and leaves administrative states unresolved. Sharp bounds
avoid outcome-conditioned deletion but may be wide. Third, the standardized
payoff omits amortization, prepayment, recoveries, fees, discounting, and
capital costs. Fourth, the 40-month gap does not restore exchangeability; the
observed deterioration is substantial. Fifth, neither the Mondrian recipe nor
the LP controls coverage after adaptive within-group selection. Sixth, purpose
concentration and score strata are not protected classes, so the analysis is
not a fair-lending certification. Seventh, this is a retrospective code-locked
audit. Earlier project work inspected overlapping historical periods, so the
evidence must not be called a pristine lockbox or preregistered prospective
test.

The successful experiment is the tagged maturity-safe bounded protocol v2 at
source commit \texttt{78a64fe}. The online supplement records the full run,
protocol, DVC, and SHA-256 identifiers without forcing long machine IDs into
the review layout.
The execution receipt records a clean initial and final tree, Python 3.11.14,
CatBoost 1.2.10, and HiGHS 1.14.0. DVC pointers track the versioned processed
panel and model/results directories; the protected historical champion and its
manifest were neither executed nor overwritten.

The evidence builder validates the protocol tag and commit, summary and receipt
hashes, every versioned artifact, and optionally the raw-file hash. It then
regenerates the seven publication tables and three figures used here. Two
consecutive builds are byte-identical. Claim-sync tests compare the JSON
source, CSV tables, anonymous QMD body, supplement, and official IJDS TeX. The
online supplement gives exact commands, schemas, proofs, and the
historical-artifact boundary. Author-identifying repository links are withheld
from this double-anonymous manuscript and can be supplied to the editor under
the IJDS data and code policy.

\section{Conclusion}\label{sec:conclusion}

CRPTO tests a common but unsafe inference: that marginal conformal coverage
continues to protect a portfolio after an optimizer selects and reweights
loans. In a status-independent, maturity-safe, monthly credit experiment, the
answer is no. The selected conformal upper-score guardrail reduces funded
default by 2.01--4.63 percentage points relative to point PD, but loses
\$58{,}040--\$322{,}704 of standardized payoff and increases funded-set
miscoverage by 0.88--2.99 percentage points. Its default benefit comes from
reallocating capital across score strata; within-stratum selection drives the
coverage failure.

The contribution is not a universally superior CRPTO policy. It is an
auditable account of where predictive uncertainty enters a decision, how
outcome maturity and economic coherence change the evidence, and why marginal
coverage is not a decision guarantee. That narrower claim is the one supported
by the data.

\bibliographystyle{informs2014}
\bibliography{../../book/references}

\end{document}
