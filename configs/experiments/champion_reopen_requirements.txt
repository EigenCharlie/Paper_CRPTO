# Dedicated exploratory environment for the champion reopen search.
# Keep canonical CRPTO dependencies in pyproject/uv.lock frozen until a candidate
# passes promotion gates.
-e .[search]
pytest>=9.0
ruff>=0.10
probmetrics>=1.3
crepes>=0.9
netcal>=1.3
# Optional if a C compiler is available in the local environment:
# splinecalib>=0.0.13,<0.1
cir-model>=0.1
optuna>=4.0
mapie>=1.4
venn-abers>=1.5
betacal>=1.1
