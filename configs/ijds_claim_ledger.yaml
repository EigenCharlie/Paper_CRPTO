schema_version: "2026-07-26.2"
status: active_ijds_claim_contract

surfaces:
  body: paper/CRPTO_ijds.qmd
  supplement: paper/supplement_ijds.qmd
  registry: docs/research/active_claims_2026-07-14.md
  claim_matrix: paper/submission/CLAIM_AUDIT_MATRIX.md

claims:
  - id: data.exhaustive_status_independent_population
    status: active
    kind: empirical
    rule: equals
    result_pointer: /data_contract/sampling
    expected: none_all_eligible_rows_within_each_declared_temporal_role
    lineages: [raw_data_audit, binary_geometry]
    scope: declared_36_month_temporal_design
    forbidden_inference: [raw_archive_census_is_one_common_estimand, external_validity]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: endpoint.not_verified_snapshot
    status: active
    kind: boundary
    rule: equals
    result_pointer: /design/archive_is_verified_point_in_time_snapshot
    expected: false
    lineages: [raw_data_audit, binary_geometry.evaluation]
    scope: reconstructed_status_availability_at_2020_09_30
    forbidden_inference: [verified_point_in_time_snapshot, observed_operational_chargeoff_date]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: endpoint.reason_census_partitions_candidates
    status: active
    kind: empirical
    rule: equals
    result_pointer: /evaluation_endpoint/reason_census_partitions_primary_candidates
    expected: true
    lineages: [binary_geometry.evaluation]
    scope: primary_oot_reconstructed_endpoint_reason_taxonomy
    forbidden_inference: [identified_missingness_mechanism, observed_operational_event_dates]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: coverage.five_models_all_windows_below_nominal
    status: active
    kind: empirical
    rule: equals
    result_pointer: /credit_risk_controls/all_five_all_eight_upper_below_nominal
    expected: true
    lineages: [binary_geometry.evaluation, credit_controls.evaluation]
    scope: alpha_0_10_all_candidate_sharp_bounds_under_six_month_endpoint_with_five_frozen_score_strata
    forbidden_inference:
      [universal_conformal_invalidity, conformal_theorem_failure,
       exchangeability_rejection_without_formal_test, selected_model_winner,
       funded_set_validity]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: coverage.closed_taxonomies_primary_below_nominal
    status: active
    kind: empirical
    rule: equals
    result_pointer: /closed_coverage_diagnostics/all_sixty_four_primary_upper_below_nominal
    expected: true
    lineages: [binary_geometry.evaluation]
    scope: alpha_0_10_under_six_month_endpoint_two_v4_learners_by_four_frozen_taxonomies_1_2_5_10_by_eight_primary_windows
    forbidden_inference:
      [selected_taxonomy, taxonomy_winner, universal_taxonomy_robustness,
       conformal_theorem_failure, exchangeability_rejection,
       joint_block_rank_reference_extension, selected_set_validity]
    surfaces:
      required: [supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: coverage.censored_extension_mixed_stress
    status: active
    kind: empirical
    rule: equals
    result_pointer: /closed_coverage_diagnostics/censored_extension_mixed_stress_pattern
    expected: true
    lineages: [binary_geometry.evaluation]
    scope: alpha_0_10_under_six_month_endpoint_five_frozen_score_strata_two_v4_learners_eight_windows_july_to_september_2017_declared_censored_extension
    forbidden_inference:
      [selected_origin, primary_oot_evidence, independent_replication,
       prospective_validation, endpoint_missingness_mechanism,
       joint_block_rank_reference_extension, model_winner,
       conformal_theorem_failure, exchangeability_rejection_without_formal_test]
    surfaces:
      required: [supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: coverage.joint_block_rank_reference_nominal_flags
    status: active
    kind: empirical
    rule: equals
    result_pointer: /exchangeability_transport_test/thirty_one_of_forty_meet_locked_nominal_thresholds
    expected: true
    lineages: [credit_controls.evaluation, exchangeability_transport_test]
    scope: alpha_0_10_fixed_post_inspection_forty_learner_window_joint_block_intersection_nulls_under_five_frozen_score_strata
    forbidden_inference:
      [preregistration, study_wide_confirmation, global_200_stratum_fwer,
       post_selection_fwer_control, usual_single_future_point_split_conformal_null_rejected,
       exchangeability_established_by_nonflag, identified_shift_mechanism,
       inference_for_other_sensitivity_lineages]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: coverage.resolved_label_diagnostic_descriptive
    status: active
    kind: empirical
    rule: equals
    result_pointer: /conformal_set_diagnostics/all_forty_resolved_y0_coverage_above_y1
    expected: true
    lineages: [credit_controls.evaluation, conformal_set_diagnostics]
    scope: resolved_primary_oot_panel_all_five_learners_all_eight_windows
    forbidden_inference:
      [label_conditional_guarantee, all_candidate_class_coverage, label_mondrian_method,
       fairness_or_equalized_coverage]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: sensitivity.label_mondrian_complete_mixed
    status: active
    kind: empirical
    rule: equals
    result_pointer: /sensitivity/label_mondrian/mixed_category_identification_states
    expected: true
    lineages: [label_mondrian.outcome_free, label_mondrian.evaluation]
    scope: alpha_0_10_all_five_learners_eight_windows_five_score_strata_two_labels
    forbidden_inference:
      [restored_temporal_transport, label_conditional_validity, category_tests,
       fairness_or_equalized_coverage, selected_method, funded_set_validity]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: sensitivity.label_mondrian_common_completion_gap
    status: active
    kind: empirical
    rule: equals
    result_pointer: /sensitivity/label_mondrian/all_forty_aggregate_class_gap_bounds_cross_zero
    expected: true
    lineages: [label_mondrian.evaluation]
    scope: alpha_0_10_five_score_strata_two_labels_each_sharp_gap_endpoint_optimizes_over_single_assignment_completions_shared_by_both_class_ratios_endpoints_may_differ
    forbidden_inference:
      [separate_endpoint_subtraction, conditional_coverage_equality,
       exchangeability, fairness_or_equalized_coverage]
    surfaces:
      required: [body, supplement, registry]
      allowed: [body, supplement, registry, claim_matrix]

  - id: geometry.binary_threshold_phase_characterization
    status: active
    kind: theorem
    rule: documented
    lineages: [binary_geometry.outcome_free, credit_controls.outcome_free, label_lag_sensitivity]
    scope: exact_order_statistic_of_the_two_mirror_calibration_samples_with_stated_calibration_and_target_support_conditions
    forbidden_inference:
      [finite_sample_causal_explanation, universal_threshold_transition,
       regime_criterion_without_the_below_half_calibration_condition,
       target_side_statement_without_a_target_support_condition,
       binomial_crossing_law]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: theory.coverage_band_identity
    status: active
    kind: theorem
    rule: documented
    lineages: [binary_geometry.outcome_free]
    scope: exact_two_threshold_coverage_band_identity_on_one_common_fixed_target_distribution_with_declared_endpoint_conventions
    forbidden_inference:
      [universal_continuity, threshold_distance_controls_coverage_change,
       geometric_explanation_of_the_finite_archive_shortfall,
       identified_shift_mechanism, restored_transport,
       comparison_across_different_target_panels]
    surfaces:
      required: [body, supplement, registry]
      allowed: [body, supplement, registry, claim_matrix]

  - id: coverage.common_panel_adjacent_response
    status: active
    kind: empirical
    rule: equals
    result_pointer: /common_panel_threshold_response/full_census_and_identities_verified
    expected: true
    lineages: [credit_controls.outcome_free, credit_controls.evaluation,
               common_panel_threshold_response]
    scope: fixed_primary_oot_panel_five_learners_by_five_score_strata_by_seven_adjacent_threshold_pairs_with_one_shared_binary_completion_per_unresolved_loan
    forbidden_inference:
      [preregistration, confirmation, temporal_validity_transfer,
       slope_or_continuity, threshold_distance_is_coverage_bound,
       selected_transition_or_winner, causal_mechanism,
       separate_coverage_bound_endpoint_subtraction,
       joint_attainability_of_all_cellwise_endpoints,
       stratum_sign_census_as_substantive_discovery,
       selected_or_funded_set_validity]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: theory.outcome_free_binary_set_bounds
    status: active
    kind: theorem
    rule: documented
    lineages: [binary_geometry.outcome_free]
    scope: fixed_nonnegative_allocation_binary_prediction_sets_normalized_by_positive_total_funded_exposure_under_unrestricted_loanwise_binary_outcomes
    forbidden_inference:
      [lower_endpoint_alone_is_a_miscoverage_floor,
       conditional_positive_class_coverage, selected_set_validity,
       funded_set_conformal_validity, optimizer_direction, policy_dominance]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: theory.binary_set_embedding_nonidentification
    status: active
    kind: theorem
    rule: documented
    lineages: [binary_geometry.outcome_free]
    scope: exact_endpoint_equivalence_classes_of_bounded_continuous_intervals_under_intersection_with_the_binary_label_space
    forbidden_inference:
      [preferred_embedding, calibrated_upper_endpoint_magnitude,
       optimizer_or_policy_direction, allocation_change_without_experiment,
       selected_or_funded_set_validity]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: timing.fit_label_crossing_retained
    status: active
    kind: empirical
    rule: equals
    result_pointer: /binary_phase_transition/label_lag_sensitivity/w7_to_w8_threshold_crossing_at_all_admissible_lags
    expected: true
    lineages: [label_lag_sensitivity]
    scope: lags_passing_locked_monthly_retention_rule
    forbidden_inference: [arbitrary_label_lag_invariance, causal_attribution]
    surfaces:
      required: [body, supplement, registry]
      allowed: [body, supplement, registry, claim_matrix]

  - id: timing.endpoint_six_month_reconciles_active
    status: active
    kind: empirical
    rule: equals
    result_pointer: /sensitivity/evaluation_endpoint_availability/six_month_endpoint_reconciles_to_active_evaluation
    expected: true
    lineages: [endpoint_availability, binary_geometry.evaluation, two_ruler.evaluation]
    scope: complete_endpoint_availability_lag_grid
    forbidden_inference: [endpoint_selection, independent_replications]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: sensitivity.second_origin_coverage_recurrence
    status: active
    kind: empirical
    rule: equals
    result_pointer: /sensitivity/rolling_origin/all_sixteen_upper_below_nominal
    expected: true
    lineages: [binary_geometry.outcome_free, rolling_origin_individual_age_followup]
    scope: two_retrospective_april_june_origins_with_cutoffs_39_months_after_each_issue_month_end_at_calendar_month_resolution
    forbidden_inference:
      [independent_replication, temporal_invariance, prospective_validation,
       exact_day_level_loan_age, pooled_origin_estimand]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: sensitivity.missingness_encoding_recurrence
    status: active
    kind: empirical
    rule: equals
    result_pointer: /sensitivity/missingness_encoding/all_three_all_eight_upper_below_nominal
    expected: true
    lineages: [binary_geometry.evaluation, missingness_encoding]
    scope: three_feature_semantics_preserving_catboost_encodings
    forbidden_inference: [identified_missingness_mechanism, encoding_winner, portfolio_robustness]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: sensitivity.fit_label_completion_coverage_recurrence
    status: active
    kind: empirical
    rule: equals
    result_pointer: /sensitivity/fit_label_completion/all_scenarios_all_windows_upper_below_nominal
    expected: true
    lineages: [binary_geometry.evaluation, fit_label_completion]
    scope: observed_only_plus_three_declared_fit_label_completion_rules
    forbidden_inference: [sharp_completion_bounds, arbitrary_label_assignment_robustness, preregistration]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: geometry.fit_label_completion_crossing_not_universal
    status: active
    kind: boundary
    rule: equals
    result_pointer: /sensitivity/fit_label_completion/w7_w8_crossing_in_all_scenarios
    expected: false
    lineages: [binary_geometry.outcome_free, fit_label_completion]
    scope: stratum_2_w7_w8_path_over_four_fit_label_scenarios
    forbidden_inference: [universal_threshold_transition, causal_explanation, geometry_invariance]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: timing.fit_and_endpoint_lags_not_factorial
    status: active
    kind: boundary
    rule: equals
    result_pointer: /sensitivity/evaluation_endpoint_availability/fit_label_lag_crossed_factorially
    expected: false
    lineages: [label_lag_sensitivity, endpoint_availability]
    scope: two_separate_one_factor_sensitivities
    forbidden_inference: [joint_lag_robustness, full_factorial_robustness]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: decision.no_selected_policy
    status: active
    kind: boundary
    rule: equals
    result_pointer: /decision_challenger/interpretation/policy_winner
    expected: null
    lineages: [two_ruler.outcome_free, two_ruler.evaluation, binary_geometry.evaluation]
    scope: all_reported_rulers_coordinates_windows_and_supports
    forbidden_inference: [policy_winner, preferred_ruler, preferred_coordinate, universal_direction]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: comparator.registered_cap_values_all_include_zero
    status: active
    kind: empirical
    rule: equals
    result_pointer: /portfolio/registered_cap_values_all_envelopes_include_zero
    expected: true
    lineages: [binary_geometry.outcome_free, binary_geometry.evaluation]
    scope: registered_point_cap_endpoints_with_values_in_0_05_to_0_12
    forbidden_inference:
      [universal_comparator_quantification, policy_equivalence,
       exhaustive_continuous_point_cap_envelope]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: theory.sharp_common_outcome_bounds
    status: active
    kind: theorem
    rule: documented
    lineages: [binary_geometry.evaluation, two_ruler.evaluation]
    scope: binary_completion_bounds_for_candidates_and_paired_allocations
    forbidden_inference: [sampling_confidence_interval, missing_at_random_identification]
    surfaces:
      required: [body, supplement, registry]
      allowed: [body, supplement, registry, claim_matrix]

  - id: theory.binary_identification_width
    status: active
    kind: theorem
    rule: documented
    lineages: [binary_geometry.evaluation, two_ruler.evaluation]
    scope: unrestricted_binary_completion_for_common_outcome_policy_contrasts
    forbidden_inference: [missing_at_random_identification, sampling_uncertainty, causal_effect]
    surfaces:
      required: [body, supplement, registry]
      allowed: [body, supplement, registry, claim_matrix]

  - id: theory.basis_endpoint_sufficiency
    status: active
    kind: theorem
    rule: documented
    lineages: [binary_geometry.outcome_free, binary_geometry.evaluation]
    scope: one_fixed_optimal_lp_basis_range_with_continuous_interval_exactness_conditional_on_a_certified_exhaustive_partition
    forbidden_inference:
      [continuous_joint_frontier_uniqueness, arbitrary_comparator_support,
       empirical_exhaustive_continuous_basis_path]
    surfaces:
      required: [body, supplement, registry]
      allowed: [body, supplement, registry, claim_matrix]

  - id: boundary.no_selected_set_validity
    status: active
    kind: boundary
    rule: documented
    lineages: [binary_geometry, two_ruler]
    scope: candidate_conformal_audit_followed_by_outcome_free_allocation
    forbidden_inference: [selected_set_conformal_guarantee, post_selection_coverage]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: sensitivity.structure_no_universal_direction
    status: active
    kind: empirical
    rule: equals
    result_pointer: /sensitivity/portfolio_structure/every_scenario_has_adverse_default_and_miscoverage_cells
    expected: true
    lineages: [portfolio_structure, two_ruler.evaluation]
    scope: complete_budget_by_purpose_cap_by_lgd_cartesian_grid
    forbidden_inference: [scenario_winner, universal_adversity, structural_invariance, deployment]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: optimization.allocation_granularity_is_diagnostic
    status: active
    kind: boundary
    rule: equals
    result_pointer: /sensitivity/allocation_granularity/integer_policy_or_reoptimization_claim_authorized
    expected: false
    lineages: [two_ruler.outcome_free, allocation_granularity]
    scope: deterministic_usd25_floor_with_residual_cash_on_active_allocations
    forbidden_inference: [integer_optimality, discrete_policy_robustness, reoptimization]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]

  - id: optimization.finite_rhs_coverage_without_uniqueness
    status: active
    kind: boundary
    rule: equals
    result_pointer: /portfolio/policy_support_rhs_semantics/rhs_coverage_recovered_without_uniqueness_promotion
    expected: true
    lineages: [policy_support_optimal_face_v2, policy_support_rhs_semantics_recovery]
    scope: active_upper_solver_reported_rhs_ranges_plus_analytically_derived_zero_dual_basic_row_safe_rays_and_196_v2_midpoint_seeds_retrospectively_registered_in_v3a_over_support_0_05_to_0_12_at_absolute_tolerance_1e_10
    forbidden_inference:
      [exact_symbolic_optimal_face, global_optimal_face_diameter,
       numerical_or_continuous_frontier_uniqueness, allocation_continuity,
       seam_conditioning, all_optimal_allocations_enumerated, selected_cap,
       exact_distinct_optimum, exact_nonuniqueness,
       exact_continuous_outcome_envelope, universal_comparator_support]
    surfaces:
      required: [body, supplement, registry, claim_matrix]
      allowed: [body, supplement, registry, claim_matrix]
